import styled from "styled-components/macro";
import { GoogleMap, useLoadScript, Marker } from "@react-google-maps/api";

export const StyledMap = styled(GoogleMap)`
  width: 100%;
  height: 100vh;
`;
