// import { useGeoLocation } from "use-geo-location";
// import { getGeoPositionKey } from "../../api/AccuweatherAPI/AccuweatherAPI";
// import { useQuery } from "@tanstack/react-query";

// const getLocationKey = () => {
//   const { latitude, longitude, loading, error, timestamp, googleMapsResults } =
//     useGeoLocation();

//   console.log(latitude);
//   console.log(longitude);
//   console.log(loading);
//   console.log(error);

//   const lat = latitude;
//   const lng = longitude;

//   const { data } = useQuery(["LocationKey"], () => getGeoPositionKey(lat, lng));

// };

// export default getLocationKey;
